# SecureMedia AI Agent Context

Before making project changes, read:

- `obsidian/SecureMedia AI Vault/Codex Start Here.md`

Use that note as the project handoff. It summarizes the current architecture, commands, deployment, deliverables, risks, and where the important files live.

Do not commit or expose secrets. `backend/.env` is local only and must stay ignored.
